# 2026-02-28

- 用户让助手排查“阿里云服务器提示蠕虫病毒”事件，明确同意先做**只读取证**，避免先做破坏性操作。
- 取证结论（阶段性）：暂未发现典型蠕虫/矿工落地证据（未见 xmrig/kdevtmpfsi/kinsing，cron/systemd 未见明显恶意持久化）。
- 发现高危事实：`sshd_config` 为 `PermitRootLogin yes` + `PasswordAuthentication yes`；`auth.log` 出现大量 SSH 爆破/失败，并有多来源 IP 对 `root` 的密码成功登录记录。
- 重点异常会话：2026-02-28 17:38 从 `27.148.40.160` 成功登录 `root@pts/2`，会话内启动了 `openclaw/openclaw-tui`（进程链：`sshd -> bash -> openclaw -> openclaw-tui`）。
- 风险判断：更像“SSH 凭据泄露/撞库后登录”而非已确认“蠕虫样本落地”；整体风险评级为高。
- 与用户约定下一步：用户选择“4 继续取证不改配置”；助手建议后续最小止血（踢可疑会话 + 临时封禁可疑 IP + 保留当前会话）待用户明确授权执行。
