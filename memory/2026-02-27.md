# 2026-02-27

- 排查 Feishu/Telegram 同时出现 `406 status 406`。
- 关键结论：报错时实际运行模型是 `cpa/kimi-k2.5`，不是当前默认的 `cpa/gpt-5.3-codex`。
- 证据（session `f9c7dd94-cf16-42b0-90e9-f4bdb7321337`）：
  - `2026-02-27T03:40:23Z` model-snapshot 记录为 `kimi-k2.5`。
  - 紧随其后 assistant 消息直接报 `errorMessage: "406 status 406"`（03:40:24Z）。
  - 用户在 Telegram 再次测试（11:40）仍触发同类 406。
- 同时存在 Feishu 渠道长期权限告警：`code=99991672`（联系人相关 scope），会干扰 Feishu 侧消息链路诊断，但本轮 406 的直接触发点仍是 `kimi-k2.5` 请求失败。
- 风险点：会话在跨渠道输入时可能回落到旧模型快照（`kimi-k2.5`），即使 `session_status` 显示当前模型为 `gpt-5.3-codex`。
- 后续建议：
  1. 在会话级强制指定 `cpa/gpt-5.3-codex` 后立即做跨渠道回归测试。
  2. 必要时重启 gateway 以清理会话运行态。
  3. Feishu 补齐 contact scopes 并重新生效授权，避免权限噪声掩盖主因。
